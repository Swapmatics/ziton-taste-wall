# Taste wall technical contract

All 45 variants pass `validate_taste_wall.py`:

- one `<h1>`
- `<nav aria-label="Primary">`
- `id="contact"` on `<footer>`
- hash targets resolve on semantic hosts
- `--muted` / `--mute` for de-emphasis (no text `opacity`)
- single `:root`, valid CSS tokens
- geometric nth-child patterns verified (see `37-chess-notation`)

Source of gate: Ziton `scripts/validate_taste_wall.py`.
